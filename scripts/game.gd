# game.gd
extends Reference
class_name Game # extends Reference, so it won't show up in the node creation dialog
const levelzero = preload("../scenes/levelzero.tscn")
const gui = preload("../scenes/gui/youwin.tscn")
